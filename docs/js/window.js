/**
 * Determine if the window is in landscape mode.
 * @returns {boolean} Returns whether the window is in landscape mode.
 * @constructor
 */
function IsHorizontal()
{
    return window.innerWidth > window.innerHeight;
}